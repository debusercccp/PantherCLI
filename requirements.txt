numpy==2.4.4
pandas==3.0.1
scikit_learn==1.8.0
rich==15.0.0
# Inference is CPU-only. On a machine without CUDA, install the CPU build to
# avoid pulling the CUDA-12 wheel (which fails at import without CUDA libs):
#   pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
tqdm==4.67.3
